<mxfile compressed="false" host="app.diagrams.net">
  <diagram id="ESP32DEVKIT" name="ESP32 DevKit V1 – Pin Summary">
    <mxGraphModel grid="1" gridSize="10" guides="1" tooltips="1">
      <root>
        <mxCell id="0"/>
        <mxCell id="1" parent="0"/>

        <!-- TITLE -->
        <mxCell id="t1" value="ESP32 DevKit V1 (USB-C) – GPIO & Function Summary"
          style="text;html=1;align=center;verticalAlign=middle;fontSize=18;fontStyle=1"
          vertex="1" parent="1">
          <mxGeometry x="200" y="20" width="700" height="40" as="geometry"/>
        </mxCell>

        <!-- POWER -->
        <mxCell id="p1" value="POWER & CONTROL&#10;&#10;
VIN / 5V : Input 5–12V (qua regulator)&#10;
3V3 : Output 3.3V (max ~600mA)&#10;
GND : Ground (common reference)&#10;
EN : Enable / Reset (LOW = reset)"
          style="rounded=1;whiteSpace=wrap;html=1"
          vertex="1" parent="1">
          <mxGeometry x="20" y="90" width="360" height="170" as="geometry"/>
        </mxCell>

        <!-- SAFE GPIO -->
        <mxCell id="g1" value="SAFE GPIO (Recommended)&#10;&#10;
GPIO13, 14, 18, 19, 21, 22, 23&#10;
GPIO25 (DAC1), GPIO26 (DAC2)&#10;
GPIO27, GPIO32, GPIO33&#10;&#10;
✔ Không ảnh hưởng boot&#10;
✔ Dùng tốt cho LED / Relay / PWM / SPI / I2C"
          style="rounded=1;whiteSpace=wrap;html=1;fillColor=#E8F5E9"
          vertex="1" parent="1">
          <mxGeometry x="420" y="90" width="360" height="220" as="geometry"/>
        </mxCell>

        <!-- BOOT -->
        <mxCell id="b1" value="BOOT / STRAPPING GPIO (CẨN THẬN)&#10;&#10;
GPIO0 : Bootloader select&#10;
GPIO2, GPIO4, GPIO5, GPIO12, GPIO15&#10;&#10;
⚠ Kéo sai mức logic → không boot / không flash"
          style="rounded=1;whiteSpace=wrap;html=1;fillColor=#FFF3E0"
          vertex="1" parent="1">
          <mxGeometry x="20" y="280" width="360" height="200" as="geometry"/>
        </mxCell>

        <!-- INPUT ONLY -->
        <mxCell id="i1" value="INPUT ONLY GPIO&#10;&#10;
GPIO34, GPIO35, GPIO36 (VP), GPIO39 (VN)&#10;&#10;
✔ Chỉ INPUT&#10;
✘ Không OUTPUT&#10;
✔ ADC1"
          style="rounded=1;whiteSpace=wrap;html=1;fillColor=#E
